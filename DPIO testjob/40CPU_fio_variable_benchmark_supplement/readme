以CPU_Utilization_vs_nbcpu.png为例。现在的绘图效果是CPU randread User，CPU randread System 两者都是一个颜色，而里面有CPU randread User，CPU randread System，DPU randread User，DPU randread System，CPU randwrite User，CPU randwrite System，DPU randwrite User，DPU randwrite System，一共8条线。我希望的是CPU randread User和DPU randread User，两条线是一个颜色，CPU randread System和DPU randread System两条线是另一个颜色。每两条线分配一个颜色。